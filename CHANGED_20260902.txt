New files:
- app\database\migrations\2026_09_02_100425_create_nominee_evidence_table.php (lines 1-47)
- app\app\Models\NomineeEvidence.php (lines 1-29)

Updated files:
- app\app\Http\Controllers\Judges\TopNomineesController.php (lines 9, 43-48, 54 - load NomineeEvidence per award/nominee)
- app\resources\views\contents\admin\judge\top_nominees.blade.php (lines 15-45, 198-201, 203-241, 341-361, 401-415 - "Evidence" button, modal, template per nominee, JS, CSS)

Data import (not a file change, run once from local scratch scripts, not checked in):
- Imported C:\Users\USER\Downloads\AFRICA GRCFinCrimePrev Africa 2026 Comprehensive Judges Review with Evidence_Sources.xlsx
  (all 6 "*Review" sheets: Achievement, Excellence, Promoters, Providers, Lifetime, Women)
  into the new nominee_evidence table, matched to existing nominees/awards by name+sector.
  991 of 1065 rows matched (93%) across 146 distinct nominees. 74 rows left unmatched
  (8 unique nominee names — either not present in our nominees table under that sector,
  or in one case ("Leadway Pensure") ambiguous because two identical-named nominee rows
  exist in the Pension Industry sector) — these were skipped rather than guessed.

Update (later same day):
- app\database\migrations\2026_09_02_100425_create_nominee_evidence_table.php
  (widened primary_url/authority_url/secondary_url to TEXT; added 10 columns:
  eligibility_treatment, vote_tie_note, competition_status, adverse_screen,
  adverse_materiality, adverse_summary, adverse_event_date, adverse_source_1,
  adverse_source_2, judge_materiality_treatment)
- app\app\Models\NomineeEvidence.php ($fillable extended with the 10 new columns)
- app\resources\views\contents\admin\judge\top_nominees.blade.php
  (new CSS for .evidence-flag/.adverse-block/.adverse-badge; evidence-item template
  now renders a "Status"/"Eligibility" flag when competition_status/eligibility_treatment
  signals an exclusion or hold, plus an adverse-media block with materiality badge,
  summary, sources, and judge treatment note, when present on a criterion row)

Data import (not a file change, run once from local scratch scripts, not checked in):
- Imported C:\Users\USER\Downloads\EU GRCFinCrime 2026 Master Judges Review Verified Adverse Media.xlsx
  (Achievement Detail, Excellence Detail, Other Awards Detail sheets — 1115 criterion-level
  evidence rows) into nominee_evidence, matched to nominees/awards/sectors by name.
  Matching was upgraded from the earlier straight name-match to also handle: partial/
  abbreviated sector names (e.g. "Fintech" -> "Fintech / Regtech / Paytech"), a sector-name
  typo in our own data ("Finnacial" -> "Financial"), and award names that don't line up
  with their nominal sector (resolved via a program-wide award-name fallback that also
  corrects the sector). 3 nominees (Eurofighter, European Space Agency, Europol EFECC
  Media Team) needed a manual alias because the workbook's label differed too much from
  ours for automatic matching to trust. End result: 1115/1115 rows matched (100%).
  Ran against BOTH local databases `grcawards` and `grcawards_uk` (same schema change,
  same import) — they turned out to already mirror each other in nominees/votes counts
  before this, so both now carry identical nominee_evidence data.
